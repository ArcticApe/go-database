import Carousel from "../components/common/carousel/Carousel";

const Home = () => {
    return (
        <section className="body">
            <section className="body-header">
                <h1>Home</h1>
            </section>
            <section className="body-content-carousel">
                <Carousel />
            </section>
        </section>

    )
};

export default Home;